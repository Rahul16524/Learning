let student = {
    name : "Ravi",
    age : 23,
    isEnrolled : true
};

console.log(student["name"]);   // by using . like java we can access, variables in js also
                                // plus can also use [] to access the variables 

                                // even the lhs is not string, we need to still acess via string, 
                                // like array we can access but need to pass like a string
console.log(student.age);


console.log(student.name);


// 📍 Dot vs Bracket Notation
// Use dot notation for fixed key names
// Use bracket notation for dynamic or multi-word keys


