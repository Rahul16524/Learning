// map()

let prices = [100, 200, 300];
let taxed = prices.map(p => p * 2);

console.log(taxed);

// forEach
let pricess = [100, 200, 300];
let taxedd = prices.map(p => p * 2)
                    .forEach(x => console.log(x));

console.log(taxedd);

